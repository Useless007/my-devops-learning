# 2. Networking (การเชื่อมต่อ)

## Service (ClusterIP)
* **Definition:** "โอเปอเรเตอร์ภายใน" 📞
* **Function:** ช่วยให้ Pod คุยกันเองได้ผ่านชื่อ Service (Internal DNS)
* **Benefit:** ไม่ต้องจำ IP ของ Pod เพราะ IP Pod จะเปลี่ยนไปเรื่อยๆ ทุกครั้งที่สร้างใหม่ แต่ Service IP และ Name จะคงที่

## Ingress
* **Definition:** "ประตูหน้าบ้าน" 🚪
* **Function:** รับ Traffic จากผู้ใช้งานภายนอก (Internet) เข้ามาใน Cluster
* **Setup:** ทำงานผ่าน Domain Name (Host-based routing) หรือ Path (Path-based routing)
* **Configuration:** จำเป็นต้องมี Ingress Controller (เช่น Nginx Ingress) ทำงานอยู่เบื้องหลัง

## DNS in Kubernetes
* K8s มี DNS Server ภายใน (CoreDNS)
* รูปแบบชื่อ: `<service-name>.<namespace>.svc.cluster.local`
