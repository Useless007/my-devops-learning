# 4. Storage (พื้นที่จัดเก็บข้อมูล)

## Stateless vs Stateful
* **Stateless:** แอปพลิเคชันที่ไม่จำเป็นต้องจำ State (เช่น Web Frontend) Pod ตายแล้วเกิดใหม่ได้เลย ข้อมูลหายไม่เป็นไร
* **Stateful:** แอปพลิเคชันที่ต้องจำข้อมูล (เช่น Database) Pod ตาย ข้อมูลห้ามหาย

## Components
### PersistentVolume (PV)
* **Role:** "ฮาร์ดดิสก์จริง" 💾
* **Provisoner:** Admin เป็นคนเตรียม หรือ Cloud Provider สร้างให้ (AWS EBS, Google PD)

### PersistentVolumeClaim (PVC)
* **Role:** "ใบเบิกของ" 🎫
* **User:** Developer เขียนขอใช้พื้นที่ (ระบุขนาด, Access Mode)
* **Binding:** K8s จะจับคู่ (Bind) PVC เข้ากับ PV ที่มีคุณสมบัติตรงกัน

## Lifecycle
1. Pod เรียกใช้ PVC
2. PVC ไปผูกจอง PV
3. ข้อมูลถูกเขียนลง PV
4. **ถ้า Pod ตาย:** PV และข้อมูลยังอยู่
5. **ถ้า Pod ใหม่ขึ้นมา:** จะกลับมาต่อกับ PVC เดิม และเห็นข้อมูลเดิม
