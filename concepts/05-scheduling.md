# Concept 05: Scheduling & Node Management

การจัดการ "ที่อยู่" ของ Pods เป็นเรื่องสำคัญมากใน Kubernetes เพื่อให้มั่นใจว่าแอปของเราไปรันบนเครื่องที่เหมาะสม (เช่น เครื่องที่มี GPU, หรือเครื่องที่มีความปลอดภัยสูง)

---

## 1. Labels & Selectors
หัวใจสำคัญของการเลือกคู่ใน Kubernetes คือการแปะป้าย (Label)

* **Labels:** คือ Key-Value pair ที่เราแปะไว้ที่ Node (หรือ Pod) เพื่อบอกคุณสมบัติ
    * *ตัวอย่าง:* `hardware=gpu`, `environment=production`, `region=asia`
* **Label Selectors:** คือเงื่อนไขที่ Pod ใช้ตามหา Node ที่มีป้ายตรงกับที่ต้องการ

---

## 2. Node Selection (การเลือกเครื่อง)
เราสามารถบังคับให้ Pod ไปลง Node ที่ต้องการได้ 2 วิธีหลัก:

### A. nodeSelector (Simple & Strict)
วิธีดั้งเดิมและง่ายที่สุด คือการระบุเจาะจงไปเลยว่า "ต้องลงเครื่องที่มีป้ายนี้เท่านั้น"
* **ความยืดหยุ่น:** ต่ำ (Hard Constraint)
* **ผลลัพธ์:** ถ้าหาเครื่องไม่เจอ Pod จะติดสถานะ `Pending` ตลอดไป

### B. Node Affinity (Advanced & Flexible)
มีความฉลาดและซับซ้อนกว่า แบ่งได้ 2 โหมด:
1.  **Required (Hard):** เหมือน `nodeSelector` คือต้องมีเท่านั้น ถ้าไม่มีไม่ลง
2.  **Preferred (Soft):** พยายามหาให้หน่อย แต่ถ้าไม่มีจริงๆ ก็ลงเครื่องอื่นได้ (Pod ไม่ค้าง)
    * *เหมาะสำหรับ:* การกระจาย Load หรือการเลือกเครื่องที่ "น่าจะดีกว่า" แต่ไม่ถึงกับคอขาดบาดตาย

---

## 3. Taints & Tolerations (การกันพื้นที่)
คอนเซปต์นี้จะกลับด้านกับ Node Affinity คือใช้สำหรับ **"ไล่คนออก"** หรือ **"ห้ามเข้า"**

* **Taint (รอยมลทิน/ยาพิษ):** แปะไว้ที่ **Node** เพื่อบอกว่าเครื่องนี้ห้ามยุ่ง (เช่น เครื่อง Database, เครื่องที่มีความลับ)
    * Format: `key=value:Effect`
    * *Effects:*
        * `NoSchedule`: Pod ใหม่ห้ามเข้า (Pod เก่ายังอยู่ได้)
        * `NoExecute`: ไล่ Pod เก่าที่ไม่มีสิทธิ์ออกทันที!
* **Toleration (ภูมิต้านทาน/บัตรผ่าน):** ใส่ไว้ที่ **Pod** เพื่อบอกว่า "ฉันทนทานต่อ Taint นี้ได้" หรือ "ฉันมีบัตรผ่านนะ"

> **สรุปสั้นๆ:**
> * **Affinity:** ใช้ดึงดูด Pod เข้าหา Node (Attraction 🧲)
> * **Taints:** ใช้ผลัก Pod ออกจาก Node (Repulsion 🤮)

---
*Tags: #kubernetes #scheduling #affinity #taints*