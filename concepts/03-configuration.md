# 3. Configuration & Secrets (การตั้งค่า)

## ConfigMap
* **Purpose:** เก็บค่า Configuration ทั่วไป 📝 ที่ไม่ใช่ความลับ
* **Examples:** สีพื้นหลัง, ภาษา, URL ปลายทาง
* **Usage:** Mount เป็น Environment Variable หรือ File ใน Pod

## Secret
* **Purpose:** เก็บความลับ 🔒 ข้อมูลสำคัญ
* **Examples:** รหัสผ่าน Database, API Key, Certificates
* **Mechanism:** ข้อมูลจะถูกเข้ารหัส Base64 (แต่ถอดง่าย ควรระวังเรื่อง Access Control)
* **Usage:** เหมือน ConfigMap แต่ออกแบบมาเพื่อข้อมูล sensitive

### ข้อควรระวัง (Caveats)
* **Environment Variables:** หากแก้ค่าใน ConfigMap/Secret ที่ Mount เป็น Env Var -> **ต้อง Restart Pod ใหม่** ค่าถึงจะเปลี่ยน
* **Volume Mount:** หาก Mount เป็นไฟล์ -> ค่าจะเปลี่ยนอัตโนมัติ (แต่อาจต้องรอ sync delay)
