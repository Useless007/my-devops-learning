# 🎓 Kubernetes Day 1: The Survivor Cheatsheet

บันทึกการเรียนรู้ Kubernetes เบื้องต้น เน้นการใช้งานจริงและการเอาตัวรอดใน Cluster

## 🛠️ 1. Basic Commands (คำสั่งพื้นฐาน)
อาวุธคู่กายที่ต้องใช้ตลอดเวลา:

| Action | Command | Description |
| :--- | :--- | :--- |
| **สร้าง/อัปเดต** | `kubectl apply -f <file.yaml>` | สั่งให้ K8s สร้างหรือแก้ไข Resource ตามไฟล์ |
| **ดูรายการ** | `kubectl get pods` | ดูรายชื่อ Resource (เปลี่ยน pods เป็น deploy, svc, pvc ฯลฯ ได้) |
| **ดูไส้ใน** | `kubectl describe <type> <name>` | ดูรายละเอียดเชิงลึก ใช้หา Error หรือ Events |
| **ดู Log** | `kubectl logs <pod-name>` | ดู Output ที่แอปพ่นออกมา |
| **มุดเข้า Pod** | `kubectl exec -it <pod-name> -- sh` | เข้าไปรันคำสั่ง Linux ข้างใน Container |
| **ลบ** | `kubectl delete -f <file.yaml>` | ลบ Resource ตามไฟล์ |
| **รีสตาร์ท** | `kubectl rollout restart deploy/<name>`| บังคับให้ Deployment สร้าง Pod ชุดใหม่ (ใช้แก้ Env) |

---

## 🏗️ 2. Core Concepts (คอนเซปต์หลัก)

### 🤖 A. Workloads (การรันแอป)
* **Pod:** หน่วยเล็กที่สุด (Container หุ้มเปลือก) ห้ามตาย ห้ามหาย
* **Deployment:** ผู้จัดการ Pod ดูแลเรื่องจำนวน (Replicas) และการอัปเดตเวอร์ชัน
* **Health Checks (Probes):**
    * `livenessProbe`: ตรวจชีพจร 💓 ถ้าตาย -> **Restart ใหม่**
    * `readinessProbe`: ตรวจความพร้อม ✅ ถ้าไม่พร้อม -> **ตัด Traffic ออก**

### 🚪 B. Networking (การเชื่อมต่อ)
* **Service (ClusterIP):** ตัวกลางคุยกันภายใน Cluster (มี IP ภายใน)
* **Ingress:** ประตูหน้าบ้าน 🏠 รับแขกจาก Internet (Domain Name) ส่งต่อให้ Service
    * *Tip:* อย่าลืมแก้ `/etc/hosts` เพื่อจำลอง Domain ในเครื่องตัวเอง

### ⚙️ C. Config & Secrets (การตั้งค่า & ความลับ)
* **ConfigMap:** เก็บค่า Setting ทั่วไป 📝 (Text ธรรมดา)
* **Secret:** เก็บความลับ 🔒 (Password/Token) ข้อมูลเป็น Base64
    * **Env Variable:** ฝากค่าเป็นตัวแปรระบบ (แก้แล้วต้อง Restart Pod)
    * **Volume Mount:** ฝากเป็นไฟล์ (แก้แล้วเปลี่ยนเองอัตโนมัติ)

### 💾 D. Storage (พื้นที่เก็บข้อมูล)
* **PersistentVolume (PV):** ฮาร์ดดิสก์จริง 💿 (Admin เป็นคนเตรียม หรือ Cloud สร้างให้)
* **PersistentVolumeClaim (PVC):** ใบเบิกฮาร์ดดิสก์ 🎫 (Dev เป็นคนขอ)
* **Concept:** Pod ตาย ข้อมูลไม่หาย ถ้าเก็บไว้ใน PVC (Stateful)

---

## 🆘 3. Troubleshooting Tips (เทคนิคแก้ปัญหา)

1.  **Pod ขึ้น `CrashLoopBackOff`:**
    * แปลว่าแอปพัง รันไม่ขึ้น หรือ Error แล้วดับไป
    * 👉 **แก้:** เช็ค `kubectl logs <pod-name>`

2.  **Pod ขึ้น `Pending`:**
    * แปลว่าไม่มีที่ลง (Resource ไม่พอ) หรือ PVC ยังหาฮาร์ดดิสก์ไม่ได้
    * 👉 **แก้:** เช็ค `kubectl describe pod <pod-name>`

3.  **แก้ Secret แล้วค่าไม่เปลี่ยน:**
    * ถ้าใช้แบบ Environment Variable ค่าจะถูกฝังตอน Pod เกิด
    * 👉 **แก้:** ต้องใช้คำสั่ง `kubectl rollout restart deployment/<name>`

---
*Created by My DevOps Learning Journey*
